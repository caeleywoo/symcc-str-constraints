(set-logic ALL)
(set-option :incremental true)
(set-option :produce-models true)


(declare-fun |stdin0| () String)
(declare-fun |fread0| () Int)
(declare-fun |ite0| () String)

(assert (>= fread0 0))
(assert (not (>= fread0 20)))
(assert (= fread0 (str.len (str.substr stdin0 0 fread0))))
(assert (= fread0 (str.len (str.substr stdin0 0 19))))
(assert (not (>= fread0 21)))
(assert (= fread0 19))
(assert (>= fread0 0))
(assert (not (>= fread0 20)))
(assert (= ite0 (ite (>= (str.to_code (str.substr stdin0 0 1)) 128) "\u{ff}\u{ff}\u{ff}" "\u{0}\u{0}\u{0}")))
(assert (not (>= (str.to_code (str.substr ite0 0 1)) 256)))
(assert (not (>= (str.to_code (str.substr ite0 1 1)) 256)))
(assert (not (>= (str.to_code (str.substr ite0 2 (+ (- 2) (str.len ite0)))) 256)))
(assert (not (>= (str.to_code (str.substr stdin0 0 1)) 256)))
(assert (= (str.to_code (str.substr stdin0 0 1)) (+ (* (- 16777216) (str.to_code (str.substr ite0 0 1))) (* (- 65536) (str.to_code (str.substr ite0 1 1))) (* (- 256) (str.to_code (str.substr ite0 2 (+ (- 2) (str.len ite0))))))))

(check-sat)
